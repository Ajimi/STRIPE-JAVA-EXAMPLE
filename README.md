# STRIPE JAVA API Implementation

*   Download stripe-java https://github.com/stripe/stripe-java (Using gradle or maven)

*   In the **StripeClient.java** update the stripe.apiKey in the constructor.

*   an example in **Main.java**
